# Shared Component Nodes

One file per shared library, internal SDK, or platform service consumed by multiple repos.

**Naming:** `{YOUR_SHARED_COMPONENT}.md` — use UPPER_SNAKE_CASE for the node ID.

See `shared/graph_index.md` and `../core/HOW_TO_UPDATE.md` for the node format.
